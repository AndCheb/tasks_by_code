const a = 3; // side of the square
const b = 4; // side of the square

const S = a * b; // rectangle area
const P = (a + b) * 2; // rectangle perimeter

// a function that calculates the area of a rectangle
const rectangleArea = (x, y) => x * y;

// a function that calculates the perimeter of a rectangle
const rectanglePerimeter = (x, y) => (x + y) * 2;