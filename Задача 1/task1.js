let a = 5; // сторона квадрата
let P = a * 4; // периметр квадрата

// Функция возвращающая периметр квадрата
function sideOfSquare(a) {
  return a * 4;
}

// Решение с использованием стрелочной функции
const sideOfSquareR = a => a * 4;
