const x = 4;
const y = 3;

const sum = Math.pow(x, 2) + Math.pow(y, 2);
const difference = Math.pow(x, 2) - Math.pow(y, 2);
const multiplication = Math.pow(x, 2) * Math.pow(y, 2);
const quotient = Math.pow(x, 2) / Math.pow(y, 2);